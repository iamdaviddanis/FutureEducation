using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class camera_main : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject target;

	

    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {

    	/*float mouse=Input.GetAxis("Mouse X")*1000;
    	float mouseY=Input.GetAxis("Mouse Y")*1000;

       	transform.RotateAround(target.transform.position, Vector3.up, mouse * Time.deltaTime);
       	//transform.position=transform.position+new Vector3(0,mouseY*Time.deltaTime,0);

       	*/
    }
}
